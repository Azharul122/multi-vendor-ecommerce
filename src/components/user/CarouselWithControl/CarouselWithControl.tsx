import React from 'react'
import { Product } from '../../../types'

interface props{
    products:Product[],
    title:string,
    columns:number
}

const CarouselWithControl = ({products,title}:props) => {
  return (
    <div>

    </div>
  )
}

export default CarouselWithControl